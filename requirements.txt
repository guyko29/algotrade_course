# TASE Optimal Portfolio Analyzer
# Python 3.10–3.12 (Streamlit Community Cloud compatible)
#
# Direct dependencies only — charts use Apache ECharts via CDN (no plotly /
# streamlit-echarts package). Scikit-Learn + XGBoost power the ML expected-return
# forecasting layer that feeds Markowitz/CAPM (see expected_returns.py).

# Web UI
streamlit>=1.29.0,<2.0.0

# Configuration & HTTP
python-dotenv>=1.0.0,<2.0.0
requests>=2.32.0,<3.0.0

# Data & analytics
numpy>=1.26.0,<3.0.0
pandas>=2.2.0,<3.1.0
scipy>=1.11.0,<2.0.0
statsmodels>=0.14.0,<1.0.0
arch>=7.0.0,<9.0.0

# Machine learning — expected-return forecasting layer
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<4.0.0
